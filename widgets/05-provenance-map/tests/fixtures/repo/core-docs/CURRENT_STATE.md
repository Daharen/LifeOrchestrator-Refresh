# CURRENT_STATE (fixture)

Owns reality as it exists now. This fixture is deliberately padded past its 1 KB budget so the Provenance
Map's doc-budget flagger surfaces it as OVER BUDGET (the PB-3 doc-debt signal, auto-surfaced with zero
upkeep). The real widget compares each hot doc's actual byte size against the DOC_PROTOCOL budget table.

## Phase + active work

- Building the Collective Agent on cognitive virtual memory. Modules 0-42 + widgets 01-04 built. NEXT = i41.
- The offload / verify-cost AUDIT LOOP still holds: offload only what is cheaper to verify than to do.

## Current tests

Standing gates, in order: the off-machine cloud gate FIRST, then -Live on the Windows executor, then dev.ship.

| unit | last result | task / commit | date |
|---|---|---|---|
| #2 fs.observer | 16/16 | `c3c3c3c` | 08-05 |
| #7 model.gateway | 0.7.0 (i40): warm path 30/30 cloud + live | `a1a1a1a` | 08-06 |
| widgets/04 Fan-out Wave Dashboard | 80/80 cloud + 91/91 `-Live` (+SELFTEST_LAYOUT_OK) | `b2b2b2b` | 08-06 |

## Known failures / gotchas

Highest-value section. Do not compress a live gotcha away. Padding to exceed the tiny fixture budget so the
over-budget detector has something to flag deterministically: WinForms lays out a Dock=Top toolbar's children
only after the panel is sized; bare-local WinForms handlers lose scope (use .GetNewClosure()); keep any 5.1
worker ASCII-only; the executor heartbeat is trusted over the process list; device_stage_files can return a
stale snapshot; never git add -A; verify the real HEAD via native git, not the dev.ship committed field.

## Next expected action

i41: continue the memory glue and the audit-surface increments. Widget 05 Provenance Map is the entry vehicle.
