# START_HERE (fixture)

A small routing doc, comfortably under its 6 KB budget, so the doc-budget flagger renders it OK (green) --
the counterpoint to the deliberately over-budget CURRENT_STATE.md fixture.
