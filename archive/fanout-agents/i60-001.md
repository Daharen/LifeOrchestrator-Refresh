# Fan-out worker prompt -- worker II-BOUND-i60 (plan fo-60-d578e353, iteration 60)

You are one worker in a fan-out build of Life Orchestrator, coordinated by an orchestrator instance.
First read core-docs/START_HERE.md and core-docs/HANDOFF.md, then the docs they route you to.

## Your scoped unit
i60 Lane 1 -- A+B BOUNDED-INGEST, end-to-end (design -> in-session red-team -> implement -> automatic measurement -> tests -> ship). i60 is a DELIVERY iteration: ship working code, do NOT stop at design.

SCOPE IN: ops/retrieval/** (NEW) + ops/audit/gen-retrieval-monitor.py (UPGRADE) + the tests/fixtures you add under those. SCOPE OUT: never EDIT modules/44-project-map/** (you CALL Invoke-ProjectMap.ps1 read-only), ops/manager/** (Lane 3), ops/frontdoor/**, or core-docs (docs:[]); no map/ or generated/ writes; no model call; no network.

SPEC SOURCE (bounded reads, in order): research/2026-08-16-i60-bounded-ingest-design.md (increments A + B) FIRST; ops/audit/gen-retrieval-monitor.py (its LEDGER SCHEMA + OUTPUT ROW docstring -- preserve every field + the fail-closed ledger validation); modules/44-project-map/eval/results/I53_RESULTS.md + EFFICIENCY-i53.md (the charged-byte method = the size of what entered model-visible context per open); the RETRIEVAL PROTOCOL block in the BOOT_PACKET.

A BUILD (affordance -- make the EASY path the MEASURED path): ops/retrieval/retrieve.ps1 (pwsh7), ONE verb. Given -Q <form> -Repo <root> [-Harvest <h>] -Ledger <path>, it (a) invokes modules/44-project-map/Invoke-ProjectMap.ps1 -Action query with those args, (b) on status:ok computes charged_bytes = the UTF-8/LF-normalized byte length of the rendered result payload that entered context, (c) appends ONE valid ledger line {kind: section|card|query (derived from the --q prefix), target: the --q form, bytes: charged_bytes} to -Ledger (create if absent), (d) re-emits the query envelope on stdout unchanged. FAIL-CLOSED: a non-ok query appends NOTHING and exits non-zero. Deterministic; no model.

B BUILD (automatic measurement -- zero-bounded-opens cannot pass unnoticed): upgrade gen-retrieval-monitor.py: (1) a FAIL-CLOSED GATE -- when whole_doc_open bytes > 0 AND bounded_query_bytes == 0, exit 1 with a machine reason (zero_bounded_opens) and append NOTHING; expose it so the wave close runs it (a --gate flag or default-at-close; your design, documented). (2) an OPTIONAL --artifacts-dir cross-check over modules/30-orchestrate-fanout/runtime/artifacts/*/result.json and modules/44-project-map/runtime/artifacts/*/result.json (the wrapper persists a result.json per query): a bounded ledger entry with NO matching artifact => 'unbacked' warning; a query artifact with NO ledger entry => 'unrecorded' warning; both surfaced in the output row, NEVER silently dropped. Preserve all existing rows/fields/exit codes + the existing ledger validation.

IN-SESSION RED-TEAM (D-0119, PERMITTED inside the boundary): before finalize spawn >=1 cloud subagent tasked to REFUTE -- 'can an agent still whole-open everything and pass the monitor?' and 'can the cross-check be fooled by a fabricated ledger entry or a stale artifact?' Fold each finding into a fix or a recorded residual; report the refute-attempts + verdicts.

DEMONSTRATED DEPENDENCY: section: resolution needs Lane 2's FO-6 fix. Build + unit-test the affordance against card:/query forms that work TODAY; for a section: form, assert the affordance PASSES THROUGH whatever the wrapper returns and charges bytes correctly. The section: END-TO-END proof is an i61 adoption item, NOT your ship gate.

ACCEPTANCE (mechanical fixtures + a live run): retrieve.ps1 over a card: query emits a ledger line kind=card, exact target, bytes == a recomputed length of the returned payload (assert equality); a non-ok query appends nothing + non-zero exit; the monitor GATE: an all-whole_doc_open ledger -> exit 1 zero_bounded_opens, a ledger with >=1 bounded -> exit 0; the cross-check flags one 'unbacked' + one 'unrecorded' case on a fixture pair. -Live via exec-job.sh: run retrieve.ps1 against the REAL committed map for a card: form, feed the produced ledger to the monitor -> bounded_fraction > 0.

PRESERVE: the monitor's fail-closed ledger validation, output-row schema, and exit-code meanings (0 ok / 1 content reject / 2 usage); stdlib-only Python 3.10; the affordance writes ONLY to its -Ledger + stdout; READ-ONLY outside your write scope.

TESTS: cloud py/pwsh FIRST (monitor unit tests + the affordance dry-run over a fixture query envelope), then -Live via exec-job.sh (real card: query + monitor). Double-run determinism on any canonical bytes. 0 orphans.

GATES + SHIP: cloud green -> -Live green -> exec-job.sh devship (NAMED FILES ONLY: ops/retrieval/retrieve.ps1, ops/audit/gen-retrieval-monitor.py, the tests/** + fixtures/** you add; trailers; VERIFY the real HEAD via NATIVE git, D-0072). Assert 0 UNMANAGED orphans.

STANDING RAILS (every lane): read core-docs/START_HERE.md + core-docs/CURRENT_STATE.md first; obey SKILL_CONTRACT.md. Prefer bounded PCB queries (section:/card:/--q) over whole-doc opens -- a whole-doc open is a last resort with a recorded reason (D-0146 F-i53-eff). Acquire res.lease in gpu->git->doc order, release reverse. Do ONE unit; never touch areas outside your exclusive claim; docs:[]. Gate off-machine (cloud pwsh/py) FIRST, then ship via exec-job.sh devship (sha256+AST+tests, FAIL-CLOSED, NAMED FILES ONLY, trailers). Files reach the box via SendUserFile + device_commit_files. Any persistent llama-server launches DETACHED + is reaped; assert 0 UNMANAGED orphans. Report via -Action report -PlanId <plan> -WorkerId <id> -State done + a plain measured summary; an honest INCOMPLETE beats a false done (D-0107/D-0109).

Notes: CPU lane, slot 001. RECOMMENDED MODEL: Opus 4.8 Extra (D-0114 elevate: control-plane/core-infra semantics; the load-bearing F-i53-eff closure; design + in-session red-team + build in one unit; rework forces another wave). In-session cloud subagents PERMITTED (D-0119) for the red-team. Distinct area (ops/audit + ops/retrieval) -> 0 contention with Lanes 2/3. DEMONSTRATED DEPENDENCY: the section: end-to-end proof rides Lane 2's FO-6 fix (an i61 adoption item), NOT this ship gate. Orchestrator at fold: wire the monitor GATE into the ops close + regenerate the retrieval monitor; run E (SEALED_CHECK_47) read-only.

## Resource leases (collision safety -- res.lease #29)
Acquire these BEFORE the work they guard, in THIS order (gpu -> git -> doc); each blocks up to the wait:
```
pwsh -NoProfile -File modules/29-resource-lease/Invoke-ResLease.ps1 -Action acquire -Resource "git" -Holder "II-BOUND-i60" -TtlSeconds 1800 -WaitSeconds 900
```
Acquire returns a lease_id; keep each one. Renew before its TTL if the work runs long.
Release in REVERSE order when the guarded work is done, or immediately if you block/abort:
```
pwsh -NoProfile -File modules/29-resource-lease/Invoke-ResLease.ps1 -Action release -Resource "git" -Holder "II-BOUND-i60"
```
(Release-by-holder is shown; releasing with the exact -LeaseId is stronger.)

## Report back (cadence: on_all)
Report at least once when you finish or block. Run:
```
pwsh -NoProfile -File modules/30-orchestrate-fanout/Invoke-OrchestrateFanout.ps1 -Action report -PlanId "fo-60-d578e353" -WorkerId "II-BOUND-i60" -State done -Summary "<one line: what you did>" -PlansDir "C:\Users\just_\LifeOrchestrator-Refresh\modules\30-orchestrate-fanout\runtime\plans"
```
Use -State progress for interim updates, -State blocked with -Needs '<what you need>' if stuck, -State failed if you cannot finish.

## Ship + stop
Ship your unit through the job-runner (dev.ship). Do ONE scoped unit. Do NOT touch another worker's
module, and do NOT edit the shared core-docs the orchestrator owns -- report and let the orchestrator
mirror them (it serialises doc + git writes via res.lease). Then release your leases and report done.

