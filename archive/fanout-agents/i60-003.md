# Fan-out worker prompt -- worker MGR-VIEW-i60 (plan fo-60-d578e353, iteration 60)

You are one worker in a fan-out build of Life Orchestrator, coordinated by an orchestrator instance.
First read core-docs/START_HERE.md and core-docs/HANDOFF.md, then the docs they route you to.

## Your scoped unit
i60 Lane 3 -- D: the bounded MANAGER/PROGRAM projection. A standalone deterministic generator over canonical state; ship in i60.

SCOPE IN: ops/manager/** (NEW: the generator + its generated view + tests/fixtures). SCOPE OUT: never edit modules/44, ops/audit, ops/retrieval, ops/frontdoor, or core-docs (docs:[]); read canonical state READ-ONLY; no model; no network. This is NOT a cumulative-doc class migration (that DEFERS to i68) and NOT a per-doc approval gate -- it is an observability/steering projection.

SPEC SOURCE (bounded reads): research/2026-08-16-i60-bounded-ingest-design.md (increment D) FIRST; the map state under modules/44-project-map/map/ (entities/*, overlay/*, relationships.json) = the PRIMARY source; reference impls ops/audit/gen-doc-health.py + ops/audit/gen-retrieval-monitor.py (row/render + --check patterns) and the BOOT_PACKET render (the bounded, count-asserted, pointer-backed precedent); CURRENT_STATE frontier + PROCESS_BACKLOG '## Open' rows for the frontier lines.

BUILD: ops/manager/gen-manager-view.py (stdlib Python 3.10, deterministic, no model/network/git call -- read files only) that renders ops/manager/generated/MANAGER_VIEW.md, a BOUNDED + COUNT-ASSERTED management/audit/steering projection: (a) iteration + phase + the one-line overlay frontier; (b) a per-PLANE module-status rollup (counts by status: mvp-complete / design-only / deferred / in-progress) computed from map entities; (c) open FRONTIER CANDIDATES / next actions from the overlay; (d) open PB rows (id + one line + trigger) parsed from PROCESS_BACKLOG; (e) live PROHIBITIONS / OPEN-RULINGS count + the SP3/M-03 + SEALED_CHECK status line; (f) a COUNTS line asserting N modules / M planes / K open-PB that MUST match the map (a mismatch fails the --check gate). Add a `--check` mode (re-render to a buffer + byte-compare the committed view; drift => non-zero, the #44 render --check pattern) and a documented size cap (<= 12,000 B; degrade the lowest-priority section first, NEVER drop the counts/frontier). Emit LF/UTF-8 canonical bytes; accept an injected --date like the sibling generators (no wall-clock nondeterminism); double-run byte-identical.

ACCEPTANCE (mechanical): double-run gen-manager-view.py byte-identical; the module count in the COUNTS line == the number of module: entities in the map; --check PASSES on a fresh render and FAILS after a 1-byte mutation; size <= cap; -Live via exec-job.sh against the REAL committed map -> MANAGER_VIEW.md renders the current i60 frontier + correct per-plane counts (re-derived from the map, NOT hardcoded; as of this tree: memory 15 / intelligence 8 / capability 43 / authority 6 / observability 7).

PRESERVE: read-only over all canonical state; write ONLY under ops/manager/; stdlib-only Python 3.10; fully deterministic.

TESTS: cloud py unit tests FIRST (rollup arithmetic, PB parse, the counts assertion, --check drift, the size cap), then -Live via exec-job.sh against the real map. 0 orphans.

GATES + SHIP: cloud green -> -Live green -> devship (NAMED FILES ONLY: ops/manager/gen-manager-view.py, ops/manager/generated/MANAGER_VIEW.md, ops/manager/tests/** + fixtures; trailers; VERIFY the real HEAD via native git). 0 orphans. NOTE: registering MANAGER_VIEW as a formal ops/frontdoor class is a CANDIDATE i61 follow-on -- do NOT edit ops/frontdoor this wave (lane boundary + the i68 class-migration boundary).

STANDING RAILS (every lane): read core-docs/START_HERE.md + core-docs/CURRENT_STATE.md first; obey SKILL_CONTRACT.md. Prefer bounded PCB queries (section:/card:/--q) over whole-doc opens -- a whole-doc open is a last resort with a recorded reason (D-0146 F-i53-eff). Acquire res.lease in gpu->git->doc order, release reverse. Do ONE unit; never touch areas outside your exclusive claim; docs:[]. Gate off-machine (cloud pwsh/py) FIRST, then ship via exec-job.sh devship (sha256+AST+tests, FAIL-CLOSED, NAMED FILES ONLY, trailers). Files reach the box via SendUserFile + device_commit_files. Any persistent llama-server launches DETACHED + is reaped; assert 0 UNMANAGED orphans. Report via -Action report -PlanId <plan> -WorkerId <id> -State done + a plain measured summary; an honest INCOMPLETE beats a false done (D-0107/D-0109).

Notes: CPU lane, slot 003. RECOMMENDED MODEL: Sonnet 5 High (D-0114 default: new generator over canonical state; suite-gated; reference impls = gen-doc-health + #44 render; ELEVATE to Opus 4.8 Extra only if Nicholas wants the projection's information-design owned more heavily). Distinct area (ops/manager) -> 0 contention. Reads map JSON directly -> independent of Lane 2's FO-6. Orchestrator at fold: regenerate MANAGER_VIEW at close alongside the monitors; consider the ops/frontdoor class-registration follow-on for i61.

## Resource leases (collision safety -- res.lease #29)
Acquire these BEFORE the work they guard, in THIS order (gpu -> git -> doc); each blocks up to the wait:
```
pwsh -NoProfile -File modules/29-resource-lease/Invoke-ResLease.ps1 -Action acquire -Resource "git" -Holder "MGR-VIEW-i60" -TtlSeconds 1800 -WaitSeconds 900
```
Acquire returns a lease_id; keep each one. Renew before its TTL if the work runs long.
Release in REVERSE order when the guarded work is done, or immediately if you block/abort:
```
pwsh -NoProfile -File modules/29-resource-lease/Invoke-ResLease.ps1 -Action release -Resource "git" -Holder "MGR-VIEW-i60"
```
(Release-by-holder is shown; releasing with the exact -LeaseId is stronger.)

## Report back (cadence: on_all)
Report at least once when you finish or block. Run:
```
pwsh -NoProfile -File modules/30-orchestrate-fanout/Invoke-OrchestrateFanout.ps1 -Action report -PlanId "fo-60-d578e353" -WorkerId "MGR-VIEW-i60" -State done -Summary "<one line: what you did>" -PlansDir "C:\Users\just_\LifeOrchestrator-Refresh\modules\30-orchestrate-fanout\runtime\plans"
```
Use -State progress for interim updates, -State blocked with -Needs '<what you need>' if stuck, -State failed if you cannot finish.

## Ship + stop
Ship your unit through the job-runner (dev.ship). Do ONE scoped unit. Do NOT touch another worker's
module, and do NOT edit the shared core-docs the orchestrator owns -- report and let the orchestrator
mirror them (it serialises doc + git writes via res.lease). Then release your leases and report done.

