# Fan-out worker prompt -- worker PCB-FO6-i60 (plan fo-60-d578e353, iteration 60)

You are one worker in a fan-out build of Life Orchestrator, coordinated by an orchestrator instance.
First read core-docs/START_HERE.md and core-docs/HANDOFF.md, then the docs they route you to.

## Your scoped unit
i60 Lane 2 -- C: FO-6 repair (the -Action query -Repo passthrough). Bounded, suite-gated; ship in i60.

SCOPE: EXCLUSIVE to modules/44-project-map/** EXCEPT never write map/, generated/, or eval/ (orchestrator-owned + re-rendered at fold). docs:[]; CPU; no model; no network.

SPEC SOURCE (bounded reads): research/2026-08-16-i60-bounded-ingest-design.md (increment C) FIRST; modules/44-project-map/Invoke-ProjectMap.ps1 -- the argv builder: the 'harvest' branch emits --repo, the 'else' branch (query/render/validate/verify) DOES NOT; that dropped passthrough IS FO-6, so query --q section:/card:/evidence: --harvest cannot resolve repo-backed content. MODULE_ROADMAP #44 FO-6 row; the RETRIEVAL PROTOCOL note that section:/card:/evidence: need --repo + --harvest; WORK_ORDER.md + SCHEMA_NOTES.md for the invariants you must preserve.

BUILD: in the non-harvest (else) branch of Invoke-ProjectMap.ps1, emit '--repo <Repo>' to the worker argv whenever -Repo is provided (mirroring the harvest branch; provider-guarded, only when $Repo is set) so -Action query -Repo passes through. The worker already accepts a top-level --repo for query. Bump skill.json 0.4.0 -> 0.4.1 with a one-line purpose addendum naming the FO-6 query -Repo passthrough. Add NO new params/actions/query names/error codes.

ACCEPTANCE (mechanical -Live against the REAL committed map + a repo root via the executor, AND as fixtures): (1) the wrapper stderr argv echo for `-Action query -Repo <root> -Harvest <h> -Q 'card:module:44'` now contains `--repo <root>`; (2) `-Action query -Repo <root> -Harvest <h> -Q 'section:<a doc entity>#<heading>'` returns the bounded section (status:ok, non-empty) where pre-fix it could not resolve; (3) `evidence:<id> --harvest` with -Repo resolves provenance; (4) REGRESSION PARITY: validate/verify/render/harvest goldens UNCHANGED (re-baseline ONLY where --repo legitimately now flows, reported with a one-line why).

PRESERVE (0.4.0 invariants, WO-normative): SKILL_CONTRACT v0.2 envelope (logical refusal = exit 0 + status:error + the closed code table; nonzero = crash); stdout = the single envelope; canonical LF/UTF-8/sorted bytes; the CLOSED query set + CLOSED error table UNCHANGED; drafts under runtime/ only; the render --check drift gate; parallel_safe:false. READ-ONLY outside modules/44 (and never map/ generated/ eval/).

TESTS: cloud pwsh suite green (add the wrapper-argv assertion as a test), then -Live via exec-job.sh: the full suite + the four acceptance probes on the real map + render --check + 0 orphans. Double-run byte-identity where goldens are touched.

GATES + SHIP: cloud green -> -Live green -> devship (NAMED FILES ONLY: modules/44-project-map/Invoke-ProjectMap.ps1, skill.json, tests/** you changed, SCHEMA_NOTES.md/README.md only if touched; NOTHING under map/ generated/ eval/; trailers; VERIFY the real HEAD via native git). 0 orphans.

STANDING RAILS (every lane): read core-docs/START_HERE.md + core-docs/CURRENT_STATE.md first; obey SKILL_CONTRACT.md. Prefer bounded PCB queries (section:/card:/--q) over whole-doc opens -- a whole-doc open is a last resort with a recorded reason (D-0146 F-i53-eff). Acquire res.lease in gpu->git->doc order, release reverse. Do ONE unit; never touch areas outside your exclusive claim; docs:[]. Gate off-machine (cloud pwsh/py) FIRST, then ship via exec-job.sh devship (sha256+AST+tests, FAIL-CLOSED, NAMED FILES ONLY, trailers). Files reach the box via SendUserFile + device_commit_files. Any persistent llama-server launches DETACHED + is reaped; assert 0 UNMANAGED orphans. Report via -Action report -PlanId <plan> -WorkerId <id> -State done + a plain measured summary; an honest INCOMPLETE beats a false done (D-0107/D-0109).

Notes: CODING lane, slot 002. RECOMMENDED MODEL: Sonnet 5 High (D-0114 default: exact-spec'd bounded wrapper fix; suite-gated fail-closed; reference impl = the harvest branch; not ratification-critical). #44 is parallel_safe:false but this is the ONLY module-44 EDITING worker this wave (Lane 3 READS the query interface, never edits #44). Orchestrator at fold: independent re-run of the four probes on the real map; no map/generated re-render needed (wrapper-only change).

## Resource leases (collision safety -- res.lease #29)
Acquire these BEFORE the work they guard, in THIS order (gpu -> git -> doc); each blocks up to the wait:
```
pwsh -NoProfile -File modules/29-resource-lease/Invoke-ResLease.ps1 -Action acquire -Resource "git" -Holder "PCB-FO6-i60" -TtlSeconds 1800 -WaitSeconds 900
```
Acquire returns a lease_id; keep each one. Renew before its TTL if the work runs long.
Release in REVERSE order when the guarded work is done, or immediately if you block/abort:
```
pwsh -NoProfile -File modules/29-resource-lease/Invoke-ResLease.ps1 -Action release -Resource "git" -Holder "PCB-FO6-i60"
```
(Release-by-holder is shown; releasing with the exact -LeaseId is stronger.)

## Report back (cadence: on_all)
Report at least once when you finish or block. Run:
```
pwsh -NoProfile -File modules/30-orchestrate-fanout/Invoke-OrchestrateFanout.ps1 -Action report -PlanId "fo-60-d578e353" -WorkerId "PCB-FO6-i60" -State done -Summary "<one line: what you did>" -PlansDir "C:\Users\just_\LifeOrchestrator-Refresh\modules\30-orchestrate-fanout\runtime\plans"
```
Use -State progress for interim updates, -State blocked with -Needs '<what you need>' if stuck, -State failed if you cannot finish.

## Ship + stop
Ship your unit through the job-runner (dev.ship). Do ONE scoped unit. Do NOT touch another worker's
module, and do NOT edit the shared core-docs the orchestrator owns -- report and let the orchestrator
mirror them (it serialises doc + git writes via res.lease). Then release your leases and report done.

Your output (project.map) will become a Verification Console item Nicholas audits.

