# FAN-OUT ORCHESTRATOR HANDOFF

**This is the ONE live handoff doc.** Rewritten IN PLACE at the end of every orchestrator session (snapshot the outgoing version to `archive/handoffs/<date>-FANOUT_ORCHESTRATOR_HANDOFF-<tag>.md` FIRST -- `DOC_PROTOCOL.md` s5). Dated `ORCHESTRATOR_HANDOFF_*.md` docs are retired; their content lives here + in `CURRENT_STATE.md` + `archive/handoffs/`.

**You are the fan-out orchestrator** -- the ONE Claude instance that scopes work units, drives `orchestrate.fanout` (#30) to emit worker prompts, and hands them to Nicholas, who dispatches each into a FRESH Cowork session. You NEVER drive another AI session (the hard D-0051 boundary) -- every lane is human-dispatched, including the frontier lane (a human-couriered pack, not a driven session).

## 0. TL;DR

- **i35 CONSUMER WIRING CLOSED (D-0100). HEAD = `616cafb` (+ this close-docs commit -- confirm with `git log -1`), clean tree.** The i34 Tier-1 hierarchy is now USABLE end-to-end through #40's PUBLIC retriever path: #40 context.compile **0.7.0** (`aa2f0fb`; the REAL `ArtifactSearchHierarchyPort` wired into `-Retriever artifact_search` -- SEAM 1 leaf hydration + SEAM 2 no-false-negative prune-certificate; flat/non-descend byte-identical to 0.6) + #37 retrieval.eval **eval 0.7.0** (`58eedcb`; NEW `rehearsal_eval.py` -- the RUNNABLE ~200MB Tier-1 acceptance-gate harness over a real click-8.1.7 sample + `FULL_CORPUS_RECIPE.md`). **The D-0077 fold PASSED** -- the WIRED public-port gate 32/32 over a REAL #36 tree + the i34 smoke 38/38 regression + the rehearsal harness 35/35 (independently re-run at HEAD). **Project `tier1_accepted` stays FALSE (honest)** -- the FULL ~200MB run against #40's WIRED descend path is the flip (an i36 unit; the harness ran #36-direct nav + #40 flat). The **P0-1 injection-suite DESIGN red-team folded** (pack `2f6aa0dc`; digest `research/2026-08-05-i35-p0-1-injection-suite-redteam.md`): NO-GO as the sole action gate without a deny-by-default action reference monitor. **NEXT = i36** (the deferred glue + the tier1 flip + the action-authz freeze + P0-1 build; section 4).
- **SUNSET COUNTDOWN (mandate 01): current_iteration 35 / iterations_to_sunset 5 -> sunset i40.** `PROCESS_MANDATE.md` header (`current_iteration`/`iterations_to_sunset`); at 0 -> REPORT_DUE + the mandatory metastability report BEFORE wave work. DECREMENT it each session (i36 -> 4). Address the mandate + PB-1/PB-2/PB-3 every session (`PROCESS_BACKLOG.md`).
- **DIRECTION (D-0080/D-0090): build the Collective Agent (cognitive virtual memory)** over the Tier-0..3 evidence-staged plan (`MEMORY_ARCHITECTURE.md` + `MEMORY_BENCHMARK.md` + the seam audit). FROZEN: supervisor/warm-pool hardening (D-0079 GATE-NO; classic detached-warm is the trusted default), generators, `video.interpret` + live composition, real-time perception (arch 27-49), broad training.
- **STANDING RULE (D-0077):** when parallel isolated workers build a schema PRODUCER and its CONSUMER against a shared design doc, the orchestrator MUST run a cross-module smoke (real producer output fed into the consumer) at fold BEFORE close. (N/A to a single-worker wave.)
- Workers use `docs:[]`; YOU mirror the shared core-docs under the `git` lease (section 7); doc rules in `DOC_PROTOCOL.md`. **Doc debt (PB-3, deadline i40):** CURRENT_STATE + MODULE_ROADMAP + this handoff are over budget -- a slim + currency pass is a named unit; this close re-slimmed the handoff toward budget.
- Deliver worker prompts + verification packets + frontier packs to Nicholas as FILES (SendUserFile). Worker briefs ALSO go into `core-docs/fanout/FANOUT_AGENT_00N.md` (mirrored to the Project) so Nicholas dispatches by telling a fresh session "read `claude/fanout/FANOUT_AGENT_00N.md` and execute it" (section 5). **Slots 001/002 were archived + RESET to EMPTY at the i35 close** (i35 briefs -> `archive/fanout-agents/i35-*.md`; slot 003 stayed EMPTY -- the frontier lane is a couriered pack, not a Cowork worker).
- Box state at handoff: section 11.

## 1. Role + hard boundary (non-negotiable)

`orchestrate.fanout` (#30) is deterministic scaffolding on `res.lease` (#29): a `gpu` lease, a `git` commit lock, `doc:<path>` ownership. YOU supply judgement (what the units are, when to fan out vs serialize). The module emits prompts; Nicholas starts a fresh session per worker; workers report; the orchestrator mirrors the core-docs. <=1 GPU worker per wave, ALWAYS. Ship every unit via `dev.ship`. The orchestrator NEVER drives another AI session (D-0051) -- the frontier lane is a couriered pack (D-0052); automated external-AI access stays OUT.

## 2. First 15 minutes: orient + verify the box

Read (Project mirrors these; disk is canonical): `core-docs/START_HERE.md`, `core-docs/CURRENT_STATE.md`, THIS doc, `modules/30-orchestrate-fanout/FANOUT_PROTOCOL.md` (the module manual; MaxParallel 3 = 1 GPU + 2 CPU is the validated ceiling for this box). When editing docs: `core-docs/DOC_PROTOCOL.md`. The gotcha corpus is owned by `CURRENT_STATE.md` -> Known failures -- read it first. For the memory subsystem: `MEMORY_ARCHITECTURE.md` (the Tier-0..3 design target) + `MEMORY_CONTRACT.md` / `CONTEXT_PACKET_CONTRACT.md` (the field-level authorities). For the warm pool + supervisor (FROZEN): `modules/07-model-gateway/WARM_POOL_DESIGN.md` section 10.

Verify the box (in `device_bash`, `cd ~/mnt/LifeOrchestrator-Refresh`):
- `cat modules/00-bootstrap-executor/runtime/control/heartbeat.json` -> `at_utc` fresh, `degraded:false`, `poll_error_streak:0`, `stuck_finalize_count:0`.
- `ls modules/29-resource-lease/runtime/leases/` -> expect no LIVE `<res>.json` lease (durable `gpu-*.fence`/`.state`/`.txn` SIBLINGS persist by design -- they are NOT a held lease).
- `git log -1 --format='%h %s'` -> confirm HEAD matches section 11 (read-only git over the mount is fine; ALL git writes go through the executor).
- `pgrep -x llama-server; pgrep -x python` -> expect none (0 UNMANAGED orphans).
`device_bash` is a Linux VM -- it CANNOT run Windows pwsh; all pwsh runs through the executor (`exec-job.sh`, section 7).

## 3. Where things stand

**Now:** modules 0-34 + the memory subsystem #35-#42 + widgets 01-04 built; the fan-out loop has run 34 iterations; the Verification Console is the trusted audit surface; the Governor's `-AutoRamp` is DEFAULT-ON (M0->M1->S0); the strong tier is Qwen3.5-9B Q5_K_M GPU-resident on b10092. The res.lease GPU-lease split is shipped, hardened, consumer-adopted + live-proven (i18-i21). The **warm pool + durable supervisor stay default-OFF + FROZEN** (D-0079 as-built red-team returned GATE = NO; classic detached-warm is the trusted default). **The Phase C video spine front half is BUILT** (#32 media.decompose + #33 track.objects 0.2.0 + #34 video.timeline 0.1.1, contract-proven end-to-end); `video.interpret` (pos 22), live composition, and the DENSE-STREAM decision gate remain.

**Iteration ledger** (one line each; detail = the D-entry; commits verifiable in git; older full lines in `archive/handoffs/`):

- **i1-i24 (D-0055..D-0081) -- the pre-memory infra + video arc:** res.lease consumers + frontier.bridge #31 + executor/watchdog hardening + Governor Phase 2/3 (`-AutoRamp` default-ON, 9B Q5_K_M) + WARM_POOL_DESIGN + Verification Console durable verdicts (i1-i13); the 4-lane wave model + warm-pool Stage-1/1.1 + the DURABLE supervisor (`cc296fc`) + portability shims + widget-04 + NEW #32 media.decompose (`5026e2c`) + #33 track.objects greedy MVP (`3264dd5`) + the SD 3.5 image tier (i14-i17); the res.lease GPU-lease-split PRIMITIVE arc R1a->R1b->R1b' (`e701328`->`2d45ffe`->`f6df675`; matrix A-K 45/45; i18-i20); the R1b CONSUMER wave (#7 0.5.0 `0877c70` + the real evictor; i21); the Phase C two-lane video wave (#33 0.2.0 `b60340c` + NEW #34 video.timeline 0.1.1 `bad9e27`; the D-0077 smoke rule adopted; i22); the SUPERVISOR-HARDENING wave (#7 0.6.0 `d289ba9`; 10 must-fixes; finding 5 CLOSED; i23); then the as-built supervisor red-team GATE = NO + the whole-project-direction pack ratifying D-0080 (i24).
- i25 `fo-25-3b718a13` (D-0082): the **WAVE 1 memory-substrate 3-lane wave** -- #35 embedding.local 0.1.0 (`99b6590`) + #36 artifact.search 0.1.0 (`30ef7bd`) + #37 retrieval.eval 0.1.0 (`687edcd`); the D-0077 embedding->artifact.search->benchmark smoke PASSED; memory red-team `12c8f539` folded GO.
- i26 (D-0083): orchestrator-only -- NEW `MEMORY_CONTRACT.md` freezes the record+provenance envelope v0.1 + embedding 0.2 + retriever 0.2 + the gates. No module commits.
- i27 `fo-27-bab47060` (D-0084): the **WAVE 2 memory-RECORDS 3-lane CPU wave** -- #36 0.2.0 (`b57d328`) + NEW #38 repo.intel (`cd53565`) + NEW #39 episode.record (`b381686`); the D-0077 fold PASSED, 2 divergences bridged.
- i28 `fo-28-45c4ad65` (D-0085): the **CONTRACT-SETTLE wave** (single CPU) -- MEMORY_CONTRACT A1 (v0.1.1) + episode.record #39 0.1.1 (`3dab699`); the D-0077 re-smoke 30/30, 0 bridging.
- i29 `fo-29-87dbfa0b` (D-0086): the **WAVE 3 3-lane wave** -- NEW #40 context.compiler 0.1.0 (`b89eda0`) + #37 0.2.0 (`dc293ef`) + NEW #41 skill.card 0.1.0 (`1eafd2c`); the D-0077 fold PASSED on real data; frontier design red-team `d57fead3` FOLDED (GO read-only, NO-GO freeze/side-effect until P0-1).
- i30 `fo-30-dd453156` (D-0087/D-0088): the **CONTRACT-HARDENING wave** -- context_packet/0.2 (NEW `CONTEXT_PACKET_CONTRACT.md` + MEMORY_CONTRACT A2/A3) + #40 0.2.0 (`f06e6e7`) + #37 0.3.0 (`99bb627`; `selpol_rrf_v1`) + #41 0.2.0 (`54c2e79`); the D-0077 fold caught a selpol producer/consumer divergence (neither a defect).
- i31 `fo-31-eca37c08` (D-0089/D-0091): the **SELECTION-POLICY SETTLE wave** (single CPU) -- CONTEXT_PACKET_CONTRACT s4 PINNED to #37's canonical `selpol_rrf_v1`/1.0.0; #40 0.3.0 (`b541df6`) imports it (reference retired); the D-0077 selpol fold PASSED. D-0090 adopted the full MEMORY ARCHITECTURE as the design target; DECISION_LOG_INDEX slimmed.
- i32 `fo-32-0fb25203` (D-0092): the **Tier-0 seam-repair wave** -- MEMORY_CONTRACT A4 + CONTEXT_PACKET_CONTRACT i32 (`b8c9a3d`) + the 3-lane conformance wave (#36 0.3.0 `ef667f7`, #37 selpol 1.1 / eval 0.4 `26224d7`, #40 0.4.0 `31ebdf7`). The frontier Tier-0 red-team `159e9cb5` (D-0095) returned NO-GO-to-close-as-written -> i33.
- i33 `fo-33-d7b55e46` (D-0096/D-0097): the **NAMESPACE-CLOSURE + SUPERSESSION-HARDENING re-amendment** -- MEMORY_CONTRACT A5 + CONTEXT_PACKET_CONTRACT i33 (`41410d8`) + the re-conformance wave (#36 0.4.0 `b754b61`, #37 selpol 1.2.0 / eval 0.5 `37e5dd3`, #40 0.5.0 `c497d1b`; HEAD `c497d1b`); the D-0077 mixed-namespace fold smoke PASSED fail_count=0 (sanitized fail-closed closure; 0 leakage). ONE canonical `ns_permitted`.
- **i34 (D-0098/D-0099): Tier-1.** PART 1: MEMORY_CONTRACT **A6** + CONTEXT_PACKET_CONTRACT **i34** (folds the b4c90545 design red-team's SAFE-PRUNING P0). Two lanes shipped -- **Lane A** plan `fo-34-584fd656` (the bounded-fanout HIERARCHY nav layer: #36 0.5.0 `356ab64` + #40 0.6.0 `3968c96` + #37 eval 0.6.0 `ad20fa6`) + **Lane B** the ORPHANED plan `fo-34-45fcbd0d` from the previous stuck orchestrator (the per-task working-memory STORE: NEW #42 0.1.0 `601a2db`). **HEAD = `ad20fa6`.** The D-0077 HIERARCHY fold smoke PASSED 38/38; Tier-1 ACCEPTANCE gated on the ~200MB rehearsal (#37 tier1_accepted=false on synthetic). NEXT = i35 consumer wiring + query router.
- **i35 (D-0100): CONSUMER WIRING.** 2-lane CPU wave (plan `fo-35-0a5bf334`, GPU skipped) + a P0-1-design frontier lane. **Lane A** #40 context.compile 0.6->**0.7.0** (`aa2f0fb`; the REAL hierarchy_port into the PUBLIC `-Retriever artifact_search` path + SEAM1 leaf hydration + SEAM2 prune-cert; a NEW public-port gate 32/32 over a real #36 tree) + **Lane B** #37 retrieval.eval eval 0.6->**0.7.0** (`58eedcb`; the RUNNABLE ~200MB Tier-1 rehearsal harness + a real click-8.1.7 sample + FULL_CORPUS_RECIPE). The D-0077 fold PASSED (32/32 + 38/38 regression + 35/35). Project tier1_accepted STAYS FALSE (the full run vs the WIRED descend path is the i36 flip). The frontier P0-1 injection-suite design digest folded. Units 2/3 (#40<->#42 working_memory; the query router) DEFERRED to i36.

Runtime paths: plans `.../30-orchestrate-fanout/runtime/plans/<plan_id>/` · artifacts `.../runtime/artifacts/<id>/` · leases `.../29-resource-lease/runtime/leases/`. Waves + ad-hoc commits share one counter; **the next wave is iteration 36.**

## 4. Current frontier -- i35 CLOSED (D-0100); NEXT = i36 CONSUMER GLUE + Tier-1 flip + P0-1 pre-work

**STATUS.** i35 wired the i34 Tier-1 hierarchy into #40's PUBLIC `-Retriever artifact_search` path (the real hierarchy_port + leaf hydration + prune-certificate; fold-verified 32/32 on a real #36 tree) and turned the ~200MB rehearsal into a RUNNABLE Tier-1 acceptance-gate harness (#37 0.7.0). What remains: the last two #40-concentrated glue units (deferred by module-exclusivity), the actual Tier-1 acceptance FLIP (the full run against the WIRED descend path), and the pre-action-capable safety track (the frontier P0-1 design is now in hand). The supervisor/warm-pool freeze (D-0079) + generators + `video.interpret` + real-time perception stay FROZEN. The 4-lane wave model + the mechanics below are UNCHANGED.

**i36 candidate units** (scope distinct modules; `docs:[]`; <=1 GPU; MaxParallel <=3):

1. **#40 <-> #42 working_memory wiring** (CPU, #40). Wire #40's packet `working_memory` region to #42's `get_active_head` (today "reserved; empty"). Conjunctive task_id+namespace access; `state_version` in packet identity. (#40 -- conflicts with unit 2; sequence or one per wave.)
2. **the multi-channel query ROUTER** (CPU, #40). Realize the `query_class` stub into a real multi-channel router (query_class/temporal_intent already split + versioned; DESCEND_QUERY_CLASSES defined). (#40 -- conflicts with unit 1.) **R-1 (D-0101) BINDS HERE:** this unit MUST emit a deterministic integer-only versioned STAGE-TRACE per routing/classification/channel-selection stage (`{retrieval_plan_id, stage_id, parent_stage_id?, policy_id, policy_version, candidates_in, removed[]:{record_id|channel_id, reason_codes[]}, candidates_out, tie_break_key?}` in evaluation_hooks/diagnostics, ns-closure-checked as an i33 diagnostic array, byte-identical) -- EMISSION only, ZERO behavior change; it lands in the router's CONTEXT_PACKET_CONTRACT amendment and the D-0077 fold ASSERTS presence + determinism. "Born instrumented" -- retrofitting a shipped router contract is expensive. Design: `research/2026-08-05-interpretability-audit-surface-scoping.md` s3.
3. **the FULL ~200MB rehearsal + the `tier1_accepted` FLIP** (#37 + orchestrator). Point #37's `rehearsal_eval.py` at #40 0.7.0's WIRED descend path (override `m40_argv`; construct scoped descend-class requests -- the i35 harness ran #36-direct nav + #40 flat) and run at >=2 orders of magnitude (a distinct large corpus, or `scales` replication with the caveat noted). Flip project `tier1_accepted` ONLY if it passes; "never a silent pass."
4. **a #36 `get-record` by-rvid op** (CPU, #36). Lane A's fold reconciliation: #36 has no shipped by-rvid body-fetch op, so #40 reads the Catalog records table directly -- add a clean read op. (Small; a #36 lane.)
5. **FREEZE the action-authorization contract THEN build the P0-1 SUITE** (design-now, no coordinator). Per the frontier digest `research/2026-08-05-i35-p0-1-injection-suite-redteam.md`: freeze `lifeorch.action_proposal` + `tool_manifest` + `action_permit` + completion-predicate contracts (Gap 1, the largest blocker), THEN build the #37-style deterministic P0-1 injection SUITE (10 fixture families + 4 universal properties + a fixed-seed fuzzer + a mock authz/executor boundary + seeded-mutation kills). The hard gate before ANY action-capable release. Nothing is action-capable now (`non_execution:true` holds).
6. **Widget 05 "Provenance Map"** (CPU coding lane, D-0101 -- the audit-surface entry vehicle). Read-only WinForms construction map over the canonical docs + git dev.ship trailers + `runtime/plans/` + Verification-Console verdicts (what exists / new-since-N / verified / planned-but-unbuilt); exclusive `widgets/05-provenance-map/`, docs:[], STRICTLY read-only. A NON-DISPLACING coding-lane candidate (memory units 1-4 + the P0-1 track keep priority). Widget 06 (Compile Trace Console) follows i37-i38; Phase 2 possession is post-i40 red-team-gated. Design: `research/2026-08-05-interpretability-audit-surface-scoping.md` + `-audit-pipeline-target.md`; cadence PB-4.

**Lanes** (Nicholas's directive: up to FOUR per wave; any may be skipped; every lane human-dispatched):
- **GPU lane (<=1 per wave -- HARD CLAMP).** One worker on a GPU-bound unit; ONLY this lane touches model modules / `models.json`. Leases gpu->git.
- **CPU lane (>=1)** + **Coding lane (CPU, >=1; a DISTINCT module/area from the CPU lane).** Lease git.
- **Frontier-review lane (off-box, OPTIONAL).** NOT a Cowork worker: emit a frontier.bridge #31 `pack` (`{prompt, question?, paths?}`); Nicholas couriers it + pastes the answer back; run `read-return`, fold it. No lease; fully parallel.

**Clamps.** <=1 GPU worker always. **1 GPU + 2 CPU = MaxParallel 3** is the validated ceiling. The `git` lease serialises commits; `docs:[]` on all workers -> doc contention 0. Persistent llama-servers MUST launch DETACHED + be reaped before finalize; reassert the 0-UNMANAGED-orphan check every wave. **Schema producer/consumer pairs split across isolated workers REQUIRE the orchestrator cross-module smoke at fold (D-0077).**

**Wave loop:** scope lanes (distinct modules) + an optional frontier topic -> fill `FANOUT_AGENT_00N` slots (section 5) -> author `workers-i36.json` + `task-plan-i36.ps1` (copy an existing `task-plan-i<N>.ps1`; `-Iteration 36 -MaxParallel <=3`; `gpu:true` only on the GPU worker) -> run `plan`; confirm `dispatch_now`, <=1 gpu, 0 doc contention, clean preflight -> emit any frontier pack separately -> relay the check-in + every worker prompt + the pack as FILES + slot docs -> workers run + report -> poll `-Action status -PlanId fo-36-<id>` until `ready_for_handoff` -> `-Action handoff` -> verify commits via NATIVE git -> run any cross-module smoke the wave's shape demands -> fold, mirror the core-docs under the `git` lease, archive the used briefs + reset the slots, **regenerate the doc-health monitor** (`python ops/audit/gen-doc-health.py --date <today>` -> `ops/out/doc-health-monitor.html` + a log row in `ops/out/doc-health-log.jsonl`; SendUserFile + create_artifact it -- the D-0101/PB-4 audit tier A0.5: a READ-ONLY doc-budget + worker-context-growth dashboard, zero-upkeep, to fold into the audit sub-agent later) -> iterate.

## 5. Worker briefs: the FANOUT_AGENT slot system (D-0066)

Numbered brief docs (`FANOUT_AGENT_001..003` = GPU/CPU/coding lanes; template `FANOUT_AGENT_TEMPLATE.md`; mirrored at `claude/fanout/`) decouple "what a worker must do" from chat pasting: fill a slot at wave scoping (paste the `plan`-emitted worker prompt into its Unit section, or a tight summary pointing to the emitted copy + the governing design doc; keep the slot within its 8 KB budget), mirror it, and Nicholas dispatches a fresh session with "Read the Project doc `claude/fanout/FANOUT_AGENT_00N.md` and execute it" plus the one folder grant (section 10). Slot docs also travel to Nicholas as FILES. **The slots were archived + RESET to EMPTY at the i34 close** (i34 briefs -> `archive/fanout-agents/i34-*.md`). Lifecycle: `DOC_PROTOCOL.md` section 6.

## 6. Locations

- **Repo (canonical, git):** `C:\Users\just_\LifeOrchestrator-Refresh\` -- `core-docs/` + `modules/` + `widgets/` + `ops/` + `archive/`. Disk is canonical; the attached Claude Project mirrors `core-docs/` (mirror map in `DOC_PROTOCOL.md` section 8).
- **Large data (gitignored):** `F:\My_Programs\LifeOrchestrator-Refresh_Large_Data\` -- per-owning-module model homes; `_engines\llama.cpp` (b8661, default) + `_engines\llama.cpp-b10092` (CUDA 12.4; ONLY the 9B pins it).
- **Executor:** `modules\00-bootstrap-executor\runtime\`; driven via `exec-job.sh`. Heartbeat: `runtime/control/heartbeat.json`. Watchdog: `ops/start-watchdog.bat`.
- **Warm pool / supervisor** (`modules/07-model-gateway/`): `lib/PoolManager.psm1` + `lib/Supervisor.psm1` + `Start-GatewaySupervisor.ps1` + `lib/PoolEvictor.ps1` + `WARM_POOL_DESIGN.md` (0.6.0; the 10 must-fix hardening; `tests/Invoke-ModelGatewayHardeningTests.ps1`).
- **Video spine:** `modules/32-media-decompose/` + `modules/33-track-objects/` (0.2.0; `SCHEMA_NOTES.md` = the emitter contract) + `modules/34-video-timeline/` (0.1.1; `SCHEMA_NOTES.md` notes 34-35 = the reconciliation).
- **Memory subsystem:** `modules/35-embedding-local/` .. `modules/42-working-memory/`; each module's `SCHEMA_NOTES.md` = its contract interpretation (the D-0077 fold reads them); `core-docs/MEMORY_CONTRACT.md` + `core-docs/CONTEXT_PACKET_CONTRACT.md` = the field-level authorities; `MEMORY_ARCHITECTURE.md` = the Tier-0..3 design target.
- **Box:** `DESKTOP-PF5FFMF` -- RTX 2080 Ti 11 GB (CC 7.5), i9-9900KF 8c/16t, 64 GB RAM, Win10 Pro; C: ~67 GB free, F: 3.72 TB. Full profile: `TOOL_MODEL_REGISTRY.md`.

## 7. Mechanics cheat-sheet

- **Run pwsh via the executor** (from `device_bash`, `~/mnt/LifeOrchestrator-Refresh`): write a `task.ps1` under `modules/30-orchestrate-fanout/runtime/`, then `bash modules/00-bootstrap-executor/exec-job.sh run <id> <timeout> <task.ps1> <maxwait> "<desc>"`. Long/GPU jobs: re-run `exec-job.sh wait <id>` (device_bash caps ~45 s). Verbs: `submit|wait|run|devship|status`; `EXEC_RT` overrides the runtime dir.
- **Cloud -> device:** gate off-machine FIRST, then `SendUserFile` + `device_commit_files` the changed files onto the repo (byte-exact; <=20 MB/file). Large binaries download ON the device via an executor `curl.exe` task. `device_bash` cannot delete -- `mv` unwanted files into a `_to_delete\` folder.
- **Device -> cloud bulk reads:** `device_stage_files` (fresh, never-staged paths only -- re-staging returns a STALE snapshot; can 403 `session_stale_relogin` -> tell Nicholas, don't retry). Fallback: `tar` + `base64 -w0` + cat on-device -> slice/decode in cloud bash. Cloud has NO pwsh by default -- a GitHub tarball install (`/opt/pwsh/pwsh`, chmod +x) runs the off-machine gates.
- **Ship a unit:** `exec-job.sh devship <id> <inputs.json> <timeout>` -- dev.ship verifies sha256 + AST + tests FAIL-CLOSED, then commits ONLY the named files under the `git` lease with trailers. **VERIFY the real HEAD via native `git log`/`git show --stat`, NOT the dev.ship `committed` field** (D-0072).
- **Author a plan:** write `workers-i<N>.json` + a `task-plan-i<N>.ps1` (copy `task-plan-i23.ps1`), run it, confirm `dispatch_now` / <=1 gpu / 0 conflicts / clean preflight. `-Action status -PlanId <id>` polls; `-Action handoff` emits the Verification Console packet + next prompts. If `status` returns no artifact, read the worker reports under `plans/<id>/reports/` directly -- they are the source of truth.
- **Frontier pack:** #31 `pack` op takes `{prompt, question?, paths?}`. Emit, stage, SendUserFile; Nicholas couriers + pastes the answer BETWEEN the pack's two `<<<FRONTIER-BRIDGE-ANSWER-...>>>` markers (keep the `<!-- pack_id: ... -->` line) into the `.answer.md` return file; run `-Action read-return -ReturnFile <path> -ExpectPackId <id>` (expect `captured/valid`, `pack_id_match`) to capture; fold into a research digest + docs.
- **Doc edits + mirror (EOL-safe, fail-closed; full rules in DOC_PROTOCOL.md):** core-docs are CRLF; SOME MODULE DOCS ARE LF (e.g. `WARM_POOL_DESIGN.md`, the #34 docs) -- PRESERVE PER-FILE EOL. Pull byte-exact doc bytes (stage a FRESH path or tar/base64), apply anchored replacements in cloud (assert each anchor count == 1), `device_commit_files` the whole file back, then commit via an executor `task.ps1`: acquire the `git` lease -> `git reset -q` -> `git add -- <named files>` -> assert the staged set -> `git commit -F <msg>` -> release. Trailers: `Co-Authored-By: <acting model> <noreply@anthropic.com>` + `Claude-Session: <url>`. NEVER `git add -A`. Re-mirror via `project_write` `local_path` (inside the working dir).
- **DECISION_LOG upkeep:** append the new `D-00NN` entry at the bottom of `DECISION_LOG.md` AND append its one-row line to `DECISION_LOG_INDEX.md`; mark a superseded predecessor in its INDEX row only. Update `CURRENT_STATE.md` by REPLACING sections -- never append `[prior]` chains.
- **Deliver everything to Nicholas as FILES** (SendUserFile) -- prompts, packets, packs.

## 8. Worker-spec rules

- `docs:[]` on EVERY worker (the orchestrator mirrors core-docs; zeroes doc contention).
- <=1 GPU worker per wave; every model module is `parallel_safe:false`; ONLY the GPU lane touches `models.json` / model modules.
- Distinct module/area per worker -- never two workers in one module. **A schema PRODUCER and its CONSUMER may run as parallel isolated workers ONLY with (a) one governing design doc named as the shared contract, (b) per-module SCHEMA_NOTES.md recording every interpretation, and (c) the orchestrator cross-module smoke at fold (D-0077).**
- Correct `inputs` for any skill_id (match the skill.json op contract). A brand-new module has no skill.json yet -- OMIT `skill_id`/`skill_dir` for it.
- **Single-worker waves for core infra** (executor/watchdog, dev.ship, orchestrate.fanout itself, res.lease #29, the model.gateway durable supervisor + the in-proc res.lease client + the grown soak).
- Workers acquire leases in gpu -> git -> doc order, do ONE unit, ship via dev.ship, then `-Action report -State done`. A build-then-verify GPU unit may take git for the commit, RELEASE it, then take gpu ONLY for the live verify. A live proof whose harness itself acquires the real gpu exec/pin leases must NOT wrap an outer whole-task gpu lease around the proof (i21 lesson).

## 9. Gotchas (the load-bearing set -- full corpus: `CURRENT_STATE.md` -> Known failures)

- **The wedge (D-0055/56):** a task that BLOCKS holding a persistent llama-server orphans it + can livelock the executor while the heartbeat stays fresh. Launch persistent servers DETACHED (via the supervisor); reap before finalize; assert 0 UNMANAGED orphans. If wedged: kill the orphan OUT-OF-BAND (Task Manager -> `llama-server.exe`).
- **A long-running supervisor keeps OLD module code (i21):** RESTART the supervisor after shipping any supervisor-side change before the live check. Driver 591.74 SPILLS a too-big model to system RAM ("it loaded" != "it fits"); the measured-PEAK `required_vram` gate is the only real admission control.
- **`device_stage_files` stale snapshot:** re-staging a previously-staged path returns OLD bytes. Stage a FRESH never-staged path, or read via tar/base64 / an executor task. Staging can also 403 (`session_stale_relogin`).
- **Per-file EOL:** core-docs CRLF; some module docs (e.g. `WARM_POOL_DESIGN.md`, modules/34 docs) LF. Match the existing EOL.
- **Git discipline:** read-only git over the mount only (huge CRLF-noise M-list -- ignore it); ALL git writes through the executor under the `git` lease; NEVER `git add -A`; `project_write local_path` must be inside the working dir. `dev.ship` can FALSE-NEGATIVE `committed` (verify real HEAD via native git; clear a stale 0-byte `.git/index.lock` via an executor task, assert no `git.exe` running, then re-commit).
- **pwsh 7.4.6 determinism traps:** `[System.Array]::Sort(object[], Comparison[string])` sorts a CONVERTED COPY (silent no-op) -> cast to `[string[]]` first; empty-array unroll (`$x=@()` first); `,$out` double-wrap with `@()`; `@($list)` on a `List[object]` of pscustomobjects throws; `$var:` in a double-quoted string -> `${var}`; child-process pipe deadlock -> drain stdout+stderr async; `[Console]::Out` bypasses in-process capture. Keep double-run byte-identity gates in every canonical-bytes module.
- **Deliver files, not paths** (SendUserFile) -- and keep `-MaxParallel` at 3 until the heartbeat proves more.

## 10. Required access (grant at session start)

Every orchestrator AND worker session needs exactly ONE grant: **connect the repo folder `C:\Users\just_\LifeOrchestrator-Refresh`** (desktop app "Add folder", or approve a device_request_folder_access). That covers reading the repo, driving the executor, staging, and committing. F: is reached natively by the Windows executor. Machine prerequisite: the executor running (`ops/start-executor.bat` or the watchdog), heartbeat fresh + `degraded:false`. Computer-use (Task Manager) is only for out-of-band wedge recovery.

## 11. Box state at handoff (2026-08-05, D-0100 -- i35 CLOSED)

**i35 CONSUMER WIRING CLOSED (2026-08-05): both lanes shipped + the D-0077 fold PASSED (independently re-run at HEAD).** HEAD = **`616cafb`** (the close-B commit; chain #40 0.7.0 `aa2f0fb` -> #37 eval 0.7.0 `58eedcb` -> the D-0100 close commits `5bfa75f` / `616cafb`; THIS handoff-close commit follows -- confirm the tip with `git log -1`). Shipped (detail in DECISION_LOG D-0100 + the CURRENT_STATE tests table): #40 context.compile **0.7.0** (`aa2f0fb`; the REAL `ArtifactSearchHierarchyPort` wired into the PUBLIC `-Retriever artifact_search` path -- constructs a port over #36 Catalog/shortlist/descend/prune_verdict; SEAM1 leaf hydration [rvid -> evidence via export_chunk_texts + Catalog records]; SEAM2 no-false-negative prune-cert; V2-V5 + non_execution green; flat/non-descend byte-identical to 0.6; off-machine 322/322 + a NEW public-port gate 32/32 over a real #36 tree + -Live), #37 retrieval.eval **eval 0.7.0** (`58eedcb`; NEW `rehearsal_eval.py` [op rehearsal] -- the RUNNABLE ~200MB Tier-1 acceptance-gate harness over a committed real click-8.1.7 SAMPLE + labeled fixtures + a supersession pair + `FULL_CORPUS_RECIPE.md`; ADDITIVE, hierarchy_eval/retrieval_eval/lib byte-identical; 35/35 + 26/26). The orchestrator D-0077 fold (independently re-run at HEAD `58eedcb`) PASSED: the WIRED public-port gate 32/32 (mixed-corpus ns closure; flat byte-identical with/without catalog; deterministic packet_id covers the hierarchy body) + the i34 `smoke-i34.py` 38/38 (regression) + #37 `test_rehearsal_eval` 35/35 (fail-closed; orchestrator owns the flip). **Project-level `tier1_accepted` STAYS FALSE (honest)** -- the FULL ~200MB run against #40's WIRED descend path (the harness ran #36-direct nav + #40 flat) is the flip, an i36 unit. **Lane A recorded a FOLD RECONCILIATION** (not a blocker): #36 has no by-rvid body-fetch op, so #40 reads the Catalog records table directly -> a future #36 `get-record` op (i36 candidate 4). The **P0-1 injection-suite DESIGN red-team folded** (digest `research/2026-08-05-i35-p0-1-injection-suite-redteam.md`; NO-GO as the sole action gate without a deny-by-default action reference monitor; freeze the action-authorization contract before any action-capable coordinator). No held `res.lease` (durable `gpu-e3c5ba51.*` fence/state/txn siblings persist by design; the expired `.lease` is NOT held -- clear it before any GPU wave); heartbeat `degraded:false`; 0 UNMANAGED llama-server (1 pre-existing python pid 10312, up since 08-04, NOT i35). **PROCESS_MANDATE 01 countdown = current_iteration 35 / iterations_to_sunset 5 (sunset i40; the mandatory metastability REPORT is due then); i36 decrements to 4.** Slots 001/002 archived + RESET to EMPTY (i35 briefs -> `archive/fanout-agents/i35-*.md`); slot 003 stayed EMPTY. **NEXT = i36 CONSUMER GLUE + Tier-1 flip + P0-1 pre-work** (section 4).

**DOC-DEBT (PB-3, deadline i40):** the hot docs stay over budget (CURRENT_STATE ~53KB, MODULE_ROADMAP ~44KB, this handoff ~28KB); this close edited CURRENT_STATE + MODULE_ROADMAP + this handoff by REPLACEMENT (net-neutral). A full consolidated slim + PB-1 (the mechanical doc-hygiene GATE) remain named units; 5 iterations to the i40 sunset + the mandatory metastability report.

Prior box states + full historical iteration lines: `archive/handoffs/`.
