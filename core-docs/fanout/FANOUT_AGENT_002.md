# FANOUT_AGENT_002 -- EMPTY (reset after i33; awaiting i34 scoping)

## Header
- **Slot:** FANOUT_AGENT_002
- **Status:** EMPTY
- **Wave / iteration:** -- (next: i34)
- **Lane:** -- (GPU <=1 | CPU | CODING -- assigned at scoping)
- **Worker id / label:** --
- **Module/area (exclusive):** --
- **GPU:** false
- **Docs:** `[]`

## Mission
_Empty._ Fill at i34 wave scoping from the candidate-unit menu in
`core-docs/FANOUT_ORCHESTRATOR_HANDOFF.md` section 4 (NEXT = MEMORY_ARCHITECTURE Tier 1: the bounded-fanout
hierarchy tree + node-synopsis + shortlist-and-descend + the per-task working-memory store + the
multi-channel query router). Do NOT copy the menu here (DOC_PROTOCOL s6) -- point to it.
Format: `core-docs/fanout/FANOUT_AGENT_TEMPLATE.md`.

## Unit (the full worker prompt)
_Empty until scoped._

## Rails
Standing rules live in `core-docs/fanout/FANOUT_AGENT_TEMPLATE.md` -- copied into each brief at fill time.

## Verification
_Empty until scoped._

## Report-back record
_Empty._
