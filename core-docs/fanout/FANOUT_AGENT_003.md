# FANOUT_AGENT_003 -- EMPTY

- **Slot:** FANOUT_AGENT_003
- **Status:** EMPTY
- **Wave / iteration:** (unassigned)

No unit is currently assigned. Candidate units for the next wave live in
`FANOUT_ORCHESTRATOR_HANDOFF.md` section 4 (never copy the menu here). The orchestrator fills this slot at
wave scoping -- paste the `orchestrate.fanout` `plan`-emitted worker prompt (or a tight summary pointing to
the emitted copy + the governing design doc), mirror it to `claude/fanout/`, and Nicholas dispatches a fresh
Cowork session with "read `claude/fanout/FANOUT_AGENT_003.md` and execute it". Lifecycle: `DOC_PROTOCOL.md`
section 6.
