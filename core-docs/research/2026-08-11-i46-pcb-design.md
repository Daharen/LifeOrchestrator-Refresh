# i46 — Project Comprehension Bootstrap (PCB) design digest

**Date:** 2026-08-11 · **Iteration:** 46 · **Author:** orchestrator seat (Fable 5, Nicholas-dispatched) · **Authority:** Nicholas's comprehension/bootstrap reconstruction directive (2026-08-11) — i46 hijacked; i47 = the migration gate. **Status:** FROZEN for the i46 wave after two independent red-teams (s8). Buildable spec: `modules/44-project-map/WORK_ORDER.md` (frozen; sha in briefs). Experiment spec: `modules/44-project-map/eval/I47_EVAL_PACKET.md` (frozen at close).

## 1. Problem + objective

The legacy bootstrap ingests ~70 KB of mandatory prose (handoff + CURRENT_STATE + START_HERE + mandate) plus situational contracts (hot set ~432 KB) before knowing what matters. Build a **bounded, reconstructable comprehension layer**: L0 system map -> L1 capability cards -> L2+ retrieved detail — progressive disclosure ("know where knowledge is, not hold all knowledge"). Doctrine: *memory capacity and orchestrator-context consumption are decoupled* (MEMORY_ARCHITECTURE, applied to the project itself); *agents provide judgment; deterministic machinery owns durable structure, validation, identity, rendering* (6.6A = standing doctrine, D-0050).

## 2. Identity audit (directive s4) — VERDICT: real ambiguity; namespaces required

Confirmed: repo **module numbers** and ARCHITECTURE_MAP **positions** share bare integers with different referents (module #43 action.authz vs position 43 opaque-computer-operator; also 30/35/36/39/40/44); some modules REALIZE a different position (#36->pos 23; #32/33/34->19/20/21); widgets are a third space; naming drift exists (dir `39-episode-memory` vs id `episode.record`). **Fix:** closed namespaces, one entity per id: `module:NN/slug` (slug=skill_id) · `widget:NN/slug` · `arch:NN` (number-only key — spine positions 27-49 have no stable machine id, so prose names are display-only; red-team RT1-F3) · `plane:name` · `contract:name` · `doc:path` · `store:name` (persistent stores; RT1-F10) · `decision:D-####` · `mandate:NN` · `pb:` `iteration:` `wave:` `ops:` `future:`. Grammar + uniqueness + referential integrity validator-enforced; display aliases ("#43", "pos 43") generated. `realizes` edges link module:->arch:.

## 3. Five-plane test (directive s5) — VERDICT: sufficient with multi-membership

Memory / Intelligence / Capability / Authority / Observability-Audit fit repo evidence (D-0080 planes nest in Memory+Intelligence; audit program = Observability; #29/#43/gates/boundary = Authority). No sixth plane for v0.1, provided: `plane_primary` + `planes_secondary[]` per entity (#40 memory+intelligence; #30 intelligence+capability); assignments are evidence-pointed claims; `member-of` edges DERIVED, never authored (RT1-F10). Audit widgets observability primary; other widgets capability.

## 4. Mechanism — NEW `modules/44-project-map/` (`project.map`)

Deterministic stdlib Python (3.10-compatible) + pwsh entrypoint, the #36/#38/ops-audit pattern; READ-ONLY outside its own dirs; no model call; SKILL_CONTRACT v0.2 envelope, logical refusals in-band via a closed error-code table (RT1-F4); `parallel_safe:false`. **Canonical state** `map/` (versioned JSON, LF, sorted, pinned form): per-ns entity files + relationships (closed edge enum) + orchestrator-authored `overlay/state.json` (iteration, phase, frontier = VERBATIM handoff-s4 extract, mandate invariants, prohibitions with decision: authorities, open rulings, boot-read) — mandate cross-check on INVARIANT fields only, so the next session's countdown update cannot brick the map (RT1-F2). **Provenance per FIELD**; staleness computed per field (RT1-F8). **Judgment enters ONLY via versioned claims files** (`map_claims/0.1`, evidence-pointed, `uncertain` marked honestly; idempotent staged-swap ingest, conflicts fail closed; RT1-F6/F7). **Harvest** pulls everything mechanical (skill.json purpose/inputs/outputs/determinism/parallel_safe/requirements; doc list+budgets via imported `parse_budgets()`; owner table; D-index ids; mandate header; arch spine) — agents never re-enter machine-derivable facts (RT1-F10). **Coverage fail-closed both ways:** harvested-unit-without-entity + entity-without-backing-file are hard errors — the map cannot silently omit a new module (RT1-F9). **Stale gating:** stale load-bearing fields refuse render; >20% any-field-stale refuses; a `reaffirm` op makes re-validation a recorded event (RT1-F8/F20). `load_bearing` is DERIVED, never authorable (RT1-F15). **Generated views** (BOOT_PACKET <=20,000 B HARD, per-section budgets + recorded degradation ladder + `?`/`~` markers; L0 map; L1 cards; aliases) render only from validated state; drafts land in runtime/ only; committed generated/ is drift-checked, ships EMPTY until fold (RT1-F13/F14/F16/F23). Dirty-tree renders refuse (RT1-F18). Determinism: every array explicitly sorted; shuffle-fixture + double-run + CRLF/LF hash-normalization gates (RT1-F5/F17).

## 5. i46 wave + fold

**Lane A (coding, Opus 4.8 Extra):** build #44 to the frozen WORK_ORDER (sha in brief; unexplained deviation = FAIL). Ships harvest-seeded skeleton map, EMPTY generated/, golden fixtures. **Lane B (CPU, Sonnet 5 High):** author `claims/i46-repo-claims.json` — planes, one_lines, edges, deeper pointers, state_owned/authority_level/audit_surfaces across all namespaces, every field evidence-pointed, uncertain marked; writes claims/ ONLY (file-disjoint). Both report the WORK_ORDER sha built against; mid-wave spec changes re-brief BOTH lanes or defer (RT1-F19). **Fold (orchestrator):** independent suite re-run -> harvest -> ingest B's claims (conflicts by judgment, recorded) -> author overlay -> validate 0-errors -> close doc edits FIRST, then re-harvest + render; map+generated = FINAL close commit (RT1-F11) -> D-0077 fold smoke end-to-end -> freeze eval/. Legacy handoff untouched as control (directive 6.7); DOC_PROTOCOL owner rows for map//generated/ added at close (RT1-F24).

## 6. Freshness / rebuild / M2-C

Canonical = map/ JSON; derived = generated/; provenance = per-field source+sha256 (CRLF-normalized) + claims by/at_commit; stale = hash drift, per-field, fail-closed per s4. History stays in git/archive, never hot. **M2-C:** the map IS the routing/re-layer structure M2-C anticipates; remaining M2-C build (doc shards -> #36 records) = follow-on FO-3 (emit entities/cards through #36 `ingest_records`; CURRENT_STATE + DECISION_LOG_INDEX first candidates). Directive s13 identity convergence = FO-4+, post-pass.

## 7. i47 migration gate — pointer

Full frozen spec = `modules/44-project-map/eval/I47_EVAL_PACKET.md`. Intent: A boots legacy AT the pre-i46 commit (detached worktree — uncontaminated by i46's own docs); B boots from a BOOT_PACKET rendered against that same tree and MUST exercise validate/render step-0 (directive 6.6A); same task, same model, byte-frozen dispatches, shared probe/trap set, ledgered retrieval both sides; C scores blind-as-practical against a frozen answer key (anchored 0-4 rubric, deterministic verdict rules, two order-swapped C sessions). Legacy retires only on PASS + a later retirement gate.

## 8. Red-team record (in-session, D-0119) — two independent lenses, both NEEDS-REWORK; ALL folded

Verbatim reports: `modules/44-project-map/design/redteam-rt1.md` + `redteam-rt2.md` (committed with this digest). **RT-1 (mechanism drift/doctrine; Opus): 24 findings, 18 blocking** — headline folds: overlay-mandate cross-check to invariant-fields-only (F2); arch keys number-only (F3); SKILL_CONTRACT-conformant envelope, refusals in-band (F4); LF-normalized hashing (F5); pinned claims shape + normative fixture #0 (F6); idempotent staged-swap ingest (F7); per-FIELD provenance + reaffirm op (F8); HARVEST_ORPHAN/ENTITY_UNBACKED coverage errors (F9); directive-6.2 card fields harvested/claimed + store: ns (F10); map commit LAST at close (F11); render requires harvest (F12); drafts runtime-only (F13); SKELETON_UNRESOLVED + UNPLACED (F14); derived load_bearing + supersedes check (F15); packet section budgets + ladder + KB=1000 (F16); sorted arrays + shuffle test (F17); git facts as inputs + DIRTY_TREE (F18); pre-wave WORK_ORDER freeze + sha-in-briefs (F19); stale budget, typed deeper, packet `?`/`~` markers, owner-table rows (F20-F24). B's python3 query path verified live on the mount VM (3.10). **RT-2 (experiment validity; Opus): 20 findings, 11 blocking** — headline folds: eval tree = pre-i46 commit via detached worktree, so A's boot docs cannot describe the treatment and the freeze is operational (F1/F2); B must exercise validate/render step-0 (directive 6.6A; F3); task states the objective, not the answer (F4); frozen answer key + C restricted to key pointers (F5); absolute constraint-checklist floors (F6); out-of-band blind assignment + redacted spec + neutralized tokens, partial blinding recorded honestly (F7); same model both arms, byte-frozen dispatches (F8); anchored 0/2/4 rubric + positive PASS definition (F11); eval MANIFEST hashes + C verdict committed verbatim (F13); verbatim-extract frontier + Nicholas picks the task from 3 candidates at dispatch (F15); ledger symmetry, artifact-computable efficiency thresholds, bounded CONDITIONAL, probe/trap set, suspended-duties text, intervention/stop rules, length caps, dual order-swapped adjudicators (F9/F10/F12/F14/F16-F20).

## 9. Decisions (for the D-entry)

D-i46-1 namespaces per s2. D-i46-2 five planes accepted w/ multi-membership. D-i46-3 mechanism = `module:44/project.map`, deterministic, fail-closed, ALONGSIDE legacy (no ownership change in i46). D-i46-4 judgment only via validated evidence-pointed claims. D-i46-5 the i47 gate per the frozen packet. D-i46-6 M2-C note = s6. Follow-ons: FO-1 richer changed-since; FO-2 CURRENT_STATE.json from overlay; FO-3 map->#36 records; FO-4 identity convergence (s13); FO-5 doc-gate rows on promotion.
