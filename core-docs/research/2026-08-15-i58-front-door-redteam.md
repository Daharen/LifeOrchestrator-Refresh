# UNIVERSAL FRONT-DOOR CONTRACT -- i58 RED-TEAM (the freeze gate; confirmed breaks)

**Status: the FREEZE GATE for `2026-08-15-i58-universal-front-door-contract.md` (D-0153; 3 in-session
cloud-subagent adversaries, D-0119, one per front).** Companion: `2026-08-15-i58-front-door-hardened.md` --
the AUTHORITATIVE hardened invariant set (s8) that SUPERSEDES any naive clause of the contract (the PB-7
s4->s8 precedent, D-0149). **Verdict: the two-engine architecture + the shared-grammar seam + the
enumerable-interior re-layer are SOUND (freeze). The naive UNIVERSAL-BOUNDEDNESS claim (C1/C2 both flat;
"spill never compress"; G2 "no constraint ever silently absent"; the kernel bootstraps generator-down) is
NOT sound as stated and is CORRECTED in the companion.** Fronts: (A) universality + the judgment boundary;
(B) spill-not-compress + boundedness; (C) currency + cross-class composition + recovery. Seven confirmed
breaks; CB1 + CB4 were rated ratification-blocking.

## Confirmed breaks (ranked; front in brackets; fix -> hardened s8 rule)

- **CB1 -- the KERNEL is the wrong + insufficient recovery survivor [C, B] (ratification-blocking).** The
  i59 kernel's job is "verify/rebuild the PCB + open the GENERATED packet." When the generator is DOWN
  (producer bug, corrupt `runtime/` sqlite, render throws, the KNOWN unreproduced executor file-lock crash)
  there is no trustworthy packet AND every other surface is also a down generated front door. The kernel
  encodes generator-DEPENDENCE, then is asserted to work generator-INDEPENDENTLY (G6) -- contradiction.
  Aggravations: (a) "rebuild" = drop+rebuild needs DELETE = impossible on the read-only mount, so
  mount/Project-mirror/mobile/scheduled sessions cannot run the kernel's one instruction; (b) desktop-less
  sessions rely TODAY on hand-authored handoff+CURRENT_STATE+START_HERE, which i60 converts away -- a strict
  regression. Also [B]: the kernel cannot spill (it IS the recovery root), so its bound is COMPRESSION; its
  steps grow Theta(#classes) unless mechanized. -> rule 1.
- **CB4 -- currency does not compose across front doors [C] (ratification-blocking).** (i) No COMPILE EPOCH:
  N front doors over overlapping backings each key currency on their own `ingested_through` vs global HEAD;
  two in one boot pin to different HEADs -> a mutually inconsistent worldview with no unifying stale flag
  (the decision catalog re-ingests to 95 standing while the packet overlay still reads "94, complete"). (ii)
  Per-commit re-ingest x N NON-INCREMENTAL producers (the #45 producer drop+rebuilds the whole 653 KB
  backing; "append delta" is undefined for REPLACE-not-append docs) keyed on GLOBAL HEAD = O(Sum backings) =
  O(total knowledge) at boot -> **C1 XOR G4**. (iii) The close is NOT atomic across N steps: a partial close
  (file-lock crash) leaves a non-boot_read front door stale/truncated while G6 (boot_read-only) reports
  green; per-front-door `validate` is self-referential. -> rule 5.
- **CB2 -- the judgment carve-out swallows the universal law + re-imports F1/F2 [A].** A hand-authored
  synthesis shell fails property-1 (generated), so "universal" is rhetorical -- ~58 KB of boot surface
  (CURRENT_STATE + handoff frontier) stays hand-authored. The shell count-asserts the registry it points
  INTO, not the load-bearing SYNTHESIS: a REPLACE-don't-append rewrite silently drops a live judgment with
  every count green -- F1 for judgment, undetectable. The "load-bearing gotcha WINDOW" re-imports PB-7 F2
  ("load-bearing was a human judgement -- a determinism violation") that s8 was mandated to eliminate. No
  bright-line test -> carve-out CAPTURE. -> rules 4, 2.
- **CB3 -- "spill never compress" is retrieval-granularity + an integrity RECEIPT, not a boundedness law
  [B, C, A].** (i) Pigeonhole: B* flat AND K* flat AND "one bounded descend per class-question" cannot ALL
  hold once within-class category cardinality grows (per-module gotcha buckets, per-suite test rows,
  per-subsystem constraints are Theta(N)); at the 2-orders stress point G1 brags about, one plane's descend
  becomes a whole-plane dump. (ii) Spill is COUNTED TRUNCATION: the static packet is rendered at close with
  the action-class UNKNOWN, so it cannot "descend the branch matching the wave" -- the hot reader is as blind
  to the one binding item as under the banned slim-ladder; count-assertion buys AUDIT-completeness, not
  BOOT-sufficiency. (iii) Budget-on-prose (the shell) can be honored only by the human slim-ladder. ->
  rules 2, 3.
- **CB5 -- classifier-seam silent loss [B].** `asserted_count` is computed from the SAME keyword classifier
  (`binding_scope` from "FROZEN"/"never"/"prohibited") it is meant to audit; a standing prohibition phrased
  without a trigger keyword is tagged `ordinary`, demoted cold, and the count stays 94==94 (the independent
  count is over the same classifier). G2's "no binding constraint ever silently absent" is FALSE at the
  canonical->typed boundary. -> rule 6.
- **CB6 -- validated synopses break drop+rebuild byte-identity [A].** A model-generated synopsis regenerated
  on rebuild differs in bytes -> the double-run gate fails G3. The contract never says whether a synopsis is
  a frozen INPUT (replayed) or a derived OUTPUT (regenerated); i57 dodged it by keeping `synopsis` null. ->
  rule 7.
- **CB7 -- perpetual re-layer churn [B].** "Budget breach -> re-layer" has no fixed point in EFFORT:
  category-cardinality growth (not content) keeps pushing roots to budget, each firing a red-teamed re-layer
  wave on the MOST critical artifact; log N is the DEPTH, not the COUNT of waves -- i58 institutionalizes an
  infinite series of its own triggering event. -> rule 8.

## Held (survives -- do not re-litigate)

Enumerable-interior migration (decisions/gotchas/tests/iterations -> typed records) is SOUND; lossless
backing + expansion + abstain-on-provenance-failure hold (no path found to corrupt canonical); drop+rebuild
byte-identity AT a fixed HEAD (G3) holds (necessary, not sufficient); bounded-fanout absorbs LEAF growth into
depth; disjoint-backing front doors never skew; supersession determinism + `partially_superseded_by`
conservative over-inclusion (F3 fix) + P0-1 evidence-not-control (`can_instruct=false`) all hold. The KERNEL
exception's legitimacy rests on CONTENT-FROZEN (N-independent) -- the missing bright-line test the judgment
carve-out (hardened s8 rule 4) now borrows.

## Freeze outcome

The re-layer IS worth building; the naive universal-boundedness rule is not. The contract FREEZES with the
companion's s8 authoritative; the acceptance gate gains G7 (synthesis-completeness) + G8 (cross-front-door
consistency); **i59 is amended -- the kernel alone is insufficient, i59 also produces the generator-
independent cold-boot card + registers the uniform rebuild interface (s8 rule 1).** Detail: the companion.
