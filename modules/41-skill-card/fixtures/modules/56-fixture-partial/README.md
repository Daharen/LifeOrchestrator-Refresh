# Fixture Partial Skill

Summarize a folder of notes into a single digest for later review.

This manifest deliberately omits most section-9 fields (no purpose/inputs/requirements/invocation)
so the card generator must SURFACE the missing fields and fall back to this README for a purpose,
without crashing.
