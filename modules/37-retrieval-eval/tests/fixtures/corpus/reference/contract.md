# Skill contract

Every skill ships a manifest and returns one result envelope.

## Manifest

The manifest declares the skill id, the version, and the invocation entrypoint.

## Result envelope

The result envelope is a single json object with a schema and a status.
