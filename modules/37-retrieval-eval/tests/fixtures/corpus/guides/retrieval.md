# Retrieval and embeddings

## Recall and provenance

Retrieval quality is measured with recall and mean reciprocal rank.
Every hit must carry provenance: the source path, a content hash, and the span.

## Embedding vectors

An embedding vector is a semantic address, not a compressed document.
