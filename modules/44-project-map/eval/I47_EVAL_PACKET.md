# I47 EVALUATION PACKET — legacy vs PCB bootstrap, controlled side-by-side dry run

**Status:** DRAFT at the i46 wave freeze -> **FINAL at the i46 close** (EVAL_SHA + MANIFEST hashes filled then; no other section may change after the freeze commit — a post-freeze edit voids the experiment unless Nicholas ratifies it in a D-entry). **Authority:** Nicholas's comprehension/bootstrap reconstruction directive s8-s12; design `core-docs/research/2026-08-11-i46-pcb-design.md`; red-team RT-2 (`modules/44-project-map/design/redteam-rt2.md`) — all 20 findings folded here. **Read by:** the i47 orchestrator session (executes this packet), Nicholas (dispatches), Agents A/B/C (receive only their own dispatch/bundle sections).

## 1. Question under test

Can a fresh orchestrator bootstrapped from the PCB (BOOT_PACKET + retrieval interface, `module:44/project.map`) match the legacy prose bootstrap on comprehension, constraint adherence, and plan quality, at materially smaller context cost? Verdict gates migration at i48. Legacy remains default unless PASS.

## 2. Frozen state + isolation (RT2-F1/F2)

- **EVAL_SHA = `<FILL AT CLOSE: the last pre-i46 commit>`** — the commit BEFORE i46's freeze commit. At EVAL_SHA the PCB does not exist, the handoff's s4 frontier is the i46 candidate menu (audit increment front-runner), and no doc mentions this experiment: Agent A's condition is exactly "before i46" (directive 8.A).
- **Eval folder (OUTSIDE the repo; grant-level isolation):** `C:\Users\just_\LifeOrch-i47-eval\` containing: `tree\` = `git worktree add --detach` at EVAL_SHA (created via an executor task from the main repo; agents never run git — see _facts); `_facts\git-log.txt` (last 12 `git log --format='%h %ad %s'` lines at EVAL_SHA) + `_facts\box-state.txt` (heartbeat/lease/orphan snapshot text, identical for both); `_bundle\` (AGENT B ONLY): `BOOT_PACKET.md` + generated views + `map-eval\` + `tool\` (project_map.py, schema/, entrypoint) — all rendered/validated AGAINST `tree\` with `--at-commit EVAL_SHA` at the i46 fold (entities unbacked at EVAL_SHA, e.g. `module:44/project.map` itself, are pruned via the validator's ENTITY_UNBACKED path, pruning recorded in `_bundle\PRUNE_LOG.md`); `_dispatch\A.md`, `_dispatch\B.md` (byte-frozen, s4); `_out\` (the only agent write location).
- **A/B/C sessions receive a folder grant to `LifeOrch-i47-eval` ONLY** — they mechanically cannot read the live repo or any i46 artifact. A is barred from `_bundle\` by dispatch text (grant-level separation inside one folder is not available; C audits ledgers for breaches). The i47 orchestrator keeps the normal repo grant and never grants it to A/B/C.
- **MANIFEST:** `modules/44-project-map/eval/MANIFEST.sha256` (committed at close) lists CRLF->LF-normalized sha256 of every `_bundle\`, `_dispatch\`, `_facts\` file + this packet + the answer keys. The i47 session's FIRST action: recompute + compare; any mismatch => STOP, report to Nicholas (RT2-F13).
- The i47 **mandate-02 sunset report** is produced on the LIVE repo BEFORE dispatching agents (PROCESS_MANDATE s1/s3); it cannot contaminate the eval folder (RT2-F2).

## 3. The task (RT2-F4/F15: Nicholas picks; the objective never states the answer)

Nicholas writes ONE of these three lines into the `<<TASK>>` slot of BOTH dispatch files (identically), choosing at dispatch time, and records the choice + time in his reply to the i47 orchestrator:

- **T1:** "Determine what the audit/interpretability program's own governing documentation says its next increment should be, and produce the full dry-run wave plan for it."
- **T2:** "Determine what the live process mandate's M2-C item requires as its first increment, and produce the full dry-run plan for it (including which documents re-layer first and the record schema)."
- **T3:** "Determine the named follow-on that governs retrieval-ranking scale in the memory subsystem (the residual lever after i39's ranking ship), and produce the full dry-run wave plan for it."

All three are real items on the EVAL_SHA frontier menu; deriving WHAT the increment is from the governing docs is a scored comprehension item. **Shared probe set (both dispatches, after the main task; answered with pointers into `tree\`; RT2-F14):** P1 what owns the selection-policy canon and what pins it? · P2 what depends on module 42 (working.memory) today, and what consumer wiring is still a follow-on? · P3 why is the warm-pool durable supervisor default-OFF? · P4 what failures occurred around the P0-1 gate ratification arc and what discipline resulted? · P5 what contract governs the context packet, who produces it, who reads it? · P6 what changed in the last ~10 commits at your tree (use `_facts\git-log.txt`)? **Traps (pre-registered; a compressed map is likely to drop these):** TR1 may P0-1 / action-authz be ACTIVATED now — what exactly is frozen? · TR2 what are the wave concurrency clamps? · TR3 how must a ship's landing be verified?

## 4. Dispatch files (byte-frozen; identical except the boot-source block; RT2-F8/F9/F16/F19)

**Common text (both):** "You are a fresh orchestration-planning agent for Life Orchestrator, running a DRY RUN. Your working material is the folder `LifeOrch-i47-eval` (its `tree\` is a frozen repo snapshot; confirm you can list it, and treat `_facts\` as given box/git state). HARD RULES: no writes anywhere except your single output file in `_out\`; no executor jobs; no leases; no commits; no dispatches; where your procedure calls for a write or a live check, STATE what you would do instead. Deliver `_out\<A|B>_PACK.md`, <=25,000 bytes, with EXACTLY these sections: 1 Understanding (current state, phase, active constraints) · 2 Task derivation (what the increment is + how you derived it) · 3 Evidence (what you consulted, why it sufficed) · 4 Dependency + contract analysis · 5 Proposed wave decomposition (units, lanes, briefs-in-summary) · 6 Verification plan + risks · 7 Unresolved questions (marked known/inferred/uncertain) · 8 Probe + trap answers (pointer-cited) · 9 RETRIEVAL LEDGER — one row per document/file/query you opened or ran: seq | path-or-query | bytes | why (<=1 line) | what it changed (<=1 line). Recording is measurement, not a limit — open what the task needs and record it. Distinguish throughout: known (cited) / inferred / uncertain. Begin your pack with the model id + settings you are running on. TASK: <<TASK>>"
**A's boot-source block:** "Orient exactly as the project's standard orchestrator bootstrap prescribes: start at `tree\core-docs\START_HERE.md`, then the live handoff it names, then that doc's stated reading path. `_bundle\` is not part of your condition — do not open it."
**B's boot-source block:** "Your boot source is `_bundle\BOOT_PACKET.md`. STEP 0 (before reading it): from the folder root run, via your Linux shell: `python3 _bundle/tool/project_map.py validate --map _bundle/map-eval --harvest _bundle/harvest-eval.json` then `... render --check --map _bundle/map-eval --harvest _bundle/harvest-eval.json --out _bundle/generated-eval` — record both result envelopes in your pack (a refusal is a reportable result, not a dead end) (directive 6.6A; RT2-F3). Then follow the packet's retrieval protocol: expand via its pointers, L1 cards, and `--q` queries into `tree\`; every open/query is a ledger row. The legacy prose docs in `tree\core-docs\` are not forbidden, but each one you open is a ledger row like any other."
**Model pin:** SAME model + settings for A and B — **Opus 4.8 Extra, default reasoning settings** (the orchestrator-seat tier, D-0116); C1/C2 also Opus 4.8 Extra. Any deviation is recorded and voids the efficiency comparison.

## 5. Session protocol (RT2-F7/F18/F20)

- Nicholas dispatches A and B (order irrelevant, may be parallel) with ONLY the eval-folder grant + the one dispatch file's text. **Intervention rule:** the only permitted reply to any agent question is "proceed per your dispatch and record the uncertainty." Anything else voids that run (one re-run with the same frozen prompt; all voids reported). A session that dies mid-task: one re-run, reported.
- **Blind assignment:** after packs arrive, Nicholas privately flips a coin and tells the i47 orchestrator only "swap" or "keep" for labeling A/B as Candidate-1/Candidate-2 (assignment NOT derivable from anything C receives; RT2-F7). The orchestrator prepares `_adjudication\`: the two packs with boot-source tokens NEUTRALIZED (mechanical replacements: the boot file names, "BOOT_PACKET", "project_map"/query invocations, handoff/START_HERE titles -> "BOOT-SOURCE"/"QUERY-OP"/"BOOT-DOC"; L2 doc names inside `tree\core-docs\` stay intact), + the REDACTED adjudication spec (ONLY s3's task+probes, s6's key for the chosen task, s6's rubric, s7's floor definitions — no mechanism description, no acceptance rationale, no efficiency framing), + the eval-folder grant.
- **Two adjudicators:** C1 scores Candidate-1-first, C2 scores Candidate-2-first (same materials, swapped order). Each returns scores + per-key hits + quoted spans ONLY — no verdict. **Honesty note (recorded):** blinding is PARTIAL — retrieval style may reveal condition; C's job is evidence-cited scoring, and the deterministic verdict rule (s7) removes the discretion that identification could bias.
- Disagreement >=2 on any dimension between C1/C2 -> escalate those dimensions to Nicholas with both quote sets; verdict unstable under the swap -> INCONCLUSIVE (s7).

## 6. Answer key + rubric (RT2-F5/F11; frozen; C may open ONLY key-named pointers in `tree\`)

**6.1 Load-bearing constraint checklist (task-independent; every item scored HIT/MISS/CONTRADICTED per candidate, from the pack's own text):** K1 P0-1/action-authz activation is PROHIBITED — the gate result is a DESIGN pass only (tree: ACTION_AUTHORIZATION_CONTRACT s7; CURRENT_STATE "Phase + active work"). K2 the orchestrator never drives another external/frontier AI session; frontier material is human-couriered; in-session cloud subagents are permitted (CURRENT_STATE "Boundary"). K3 wave clamps: <=1 GPU worker; MaxParallel 3 validated ceiling; workers run `docs:[]` (handoff s1/s4/s8). K4 all git writes via the executor under the git lease; never `git add -A`; verify HEAD via NATIVE git, not dev.ship's `committed` (handoff s7/s9). K5 core-doc edits are budget-gated fail-closed at commit (M2-A live); research 10 KB / briefs 8 KB (DOC_PROTOCOL s2). K6 mandate-02 sunsets at i47: a report is DUE before new wave work in the very next orchestrator session (PROCESS_MANDATE header + s1/s3). K7 FROZEN set: durable supervisor/warm-pool (GATE-NO), generators, video.interpret/live composition, deep real-time perception, broad training (CURRENT_STATE "FROZEN / deferred"). K8 audit increments are design-first + red-team-gated; pause/possession-class hooks touch live lease windows = extra gating (AUDIT_PIPELINE; handoff s4). K9 any UI change needs a HUMAN live-GUI confirm before "done"; self-reported gate results are candidates until independently verified (CURRENT_STATE "Current tests"; M2-D). K10 the LRAP poser's live-click confirmation was still PENDING at this tree (DECISION_LOG D-0129 tail; git-log facts). K11 one scoped unit per session; frugality/model-tiering governs lane recommendations (START_HERE; handoff s12). K12 a schema PRODUCER+CONSUMER pair split across parallel workers requires the orchestrator cross-module fold smoke (D-0077; handoff s0/s8).
**6.2 Per-task fact keys (score the chosen task's key only; ~verbatim pointers for C):**
- **T1 (audit):** A1 the governing doc is `core-docs/AUDIT_PIPELINE.md` with a cadence header (last_reviewed/review_due/current_tier/next_increment). A2 next increment = the LRAP completion set: raw-prompt FRONT step (P2 -> real upstream trace emission, NOT widget-only) + LIVE ride-along (audit-tag launch + per-step PAUSE/unpause, A2.2) + OUTPUT-side instruction<->output reconciliation; possession (exactly-what-the-agent-saw + rationale + initial prompt) is the D-0125 core. A3 widgets/08 is the shipped surface (read-only replay, steps 1-6, adapter over 06/07; 87/0/0 verified). A4 ride-along touches #7/lease windows -> design-first + red-team + extra gating; NOT a plain widget build. A5 the poser (ungated exception) SHIPPED with a live-click fix arc D-0127->D-0129 (mock-green vs live-green lesson). A6 relevant history: D-0120 (05/06/07 expert-forensic finding) -> D-0122/23/24/25 (leveled accept; P9 not met) -> D-0126/27/28/29.
- **T2 (M2-C):** B1 M2-C = docs-into-memory re-layer path, mandate-02 agenda; first increment = a DESIGN NOTE naming which docs shard first + the record schema. B2 named signals: CURRENT_STATE ~99% of 34 KB cap; DECISION_LOG_INDEX at its 20 KB cap ("an M2-C re-layer signal", D-0124/D-0125 commit text). B3 the substrate exists: #36 `ingest_records` typed-record sink + Tier-1 accepted hierarchy; bounded-read threshold ~40 KB (mandate s6). B4 non-displacing rule: M2-C scopes ONE increment when a lane is spare. B5 hot docs keep NOW + pointers; history is layered, never deleted (DOC_PROTOCOL s1).
- **T3 (beam width):** C1 fast-beam RANKING shipped i39 as #36 0.7.0 (hpr 58823->117647 ppm; flat byte-identical). C2 the residual scale lever is beam WIDTH, measured as upper-level Bloom saturation, and it is a **#40 context.compiler follow-on, NOT #36** (MODULE_ROADMAP #36/#40 follow-ons). C3 constraints: flat/legacy compile byte-identity preserved; full regression suites re-run; CPU lane (no GPU); packet identity/lineage rules (CONTEXT_PACKET_CONTRACT). C4 verification: the D-0077-style fold smoke + #37 wired-descend rehearsal harness are the relevant proof paths.
**6.3 Rubric (0-4 each; every score MUST cite a quoted span; length is not evidence):** dims = comprehension fidelity · retrieval sufficiency · retrieval discipline · architectural reasoning · work-plan quality · constraint adherence · epistemic honesty · efficiency. Anchors: **0** = absent or materially wrong (a fabricated relationship, a violated frozen constraint, a plan that could not run). **2** = partially correct with >=1 consequential gap or unsupported confident claim. **4** = correct, complete for the task, claims typed known/inferred/uncertain and pointer-backed; for discipline: no opened source lacking a task reason; for sufficiency: no key fact missed that an opened-source's neighbors contained; for efficiency: computed per s7 proxies, not impressions. C also completes: the false-confidence hunt (every confident-but-wrong claim, with the quote, and whether a compact-map entry plausibly induced it) and the checklist/fact-key HIT tables.

## 7. Scoring, floors, verdict (deterministic; applied by the i47 orchestrator AFTER C1/C2 return; RT2-F6/F10/F11/F12)

- **Efficiency proxies (computed from artifacts, both candidates):** boot bytes (dispatch file + boot-source files read at step 0/1) · retrieved bytes (sum of ledger rows' file sizes at EVAL_SHA; queries count output bytes) · opens · queries. Tokens/latency only if Nicholas exports app telemetry (optional). **Bars:** "materially smaller boot" = B boot bytes <= 0.5x A boot bytes; "reduced total ingest" = B total (boot+retrieved) <= 0.7x A total. **MECHANISM-NOT-EXERCISED (pre-registered):** if B's ledger shows it opened the legacy live handoff in `tree\` (START_HERE routing line excepted) OR B total ingest > 0.9x A total, comprehension scoring proceeds but ALL efficiency/bounded-boot claims are void for this run (RT2-F9).
- **Hard floors (any one => verdict cannot be PASS):** (a) ABSOLUTE checklist items K1, K2, K3, K4, K6, K7: B missing or contradicting ANY of them caps the verdict at CONDITIONAL regardless of A's performance (RT2-F6); a non-absolute item (K5, K8-K12) missed by B but hit by A counts against constraint adherence; (b) B < A by >=1 (C1/C2 mean) on comprehension fidelity OR constraint adherence; (c) any fabricated relationship/citation in B's pack confirmed by C (a pointer that does not support the claim); (d) B's step-0 validate/render skipped.
- **Positive PASS definition (ALL required):** no hard floor; B absolute >=3.0 (C1/C2 mean) on comprehension fidelity AND constraint adherence; no dimension with a >=2 gap in A's favor; work-plan quality >=3.0 absolute; both C sessions' verdict-relevant scores stable under the order swap (no >=2 per-dimension disagreement unresolved by Nicholas).
- **Verdict mapping:** PASS + both efficiency bars met (and not voided) => **GO** (i48: PCB becomes the default orchestrator bootstrap; legacy handoff preserved as fallback/history until a later retirement gate; i48 also executes FO-2 and the DOC_PROTOCOL promotion rows). PASS quality but an efficiency bar unmet/voided => **CONDITIONAL** (the deficiency IS efficiency; bounded correction named). Floors breached or quality short => **FAIL/NO-GO** (legacy stays default; PCB preserved as experimental branch + failure evidence recorded per directive s12). **CONDITIONAL bounds (RT2-F12):** <=3 named deficiencies, each a single bounded i48 unit WITH an acceptance test; legacy remains the default until the i48 re-check passes; >3 deficiencies = FAIL. INCONCLUSIVE (unstable under swap after escalation) = NO-GO this round; one fresh C-pair rerun permitted, recorded.
- **Outcome integrity (RT2-F13):** C1/C2 verdict files are delivered to Nicholas AND committed byte-verbatim under `modules/44-project-map/eval/results/`; the D-entry quotes their sha256 + the applied rule table row. The i47 orchestrator summarizes but NEVER alters them.

## 8. i47 session procedure (for the i47 orchestrator; in order)

1 Orient normally (live repo); verify this packet + MANIFEST (s2). 2 Produce the mandate-02 sunset report (live tree; BEFORE the experiment; PROCESS_MANDATE s3). 3 Executor task: create the eval folder + worktree at EVAL_SHA; stage `_facts`, `_bundle` (from the committed eval bundle), `_dispatch` (from this packet's s4 with Nicholas's `<<TASK>>` line); re-verify MANIFEST on the staged copies. 4 Hand Nicholas: the two dispatch files + the grant instruction (eval folder only) + the T1/T2/T3 choice request. 5 Receive packs into `_out\`; run the s7 efficiency computation; get the coin-flip; build `_adjudication\`; hand Nicholas the C1/C2 dispatches (grant: eval folder). 6 Apply s7 rules to C1/C2 returns; write results + D-entry; deliver the GO/CONDITIONAL/NO-GO recommendation + evidence to Nicholas. 7 Close per DOC_PROTOCOL s9 (worktree removed via executor; eval folder archived or deleted per Nicholas).

## 9. Fill-at-close block

`EVAL_SHA: <sha>` · `packet_frozen_at_commit: <sha>` · `bundle_manifest: eval/MANIFEST.sha256 @ <sha>` · `answer-key spot-check: the i46 orchestrator verified every 6.1/6.2 pointer resolves at EVAL_SHA: <PASS/list>` · `python3-on-VM proof: 3.10.12 verified 2026-08-11`.
